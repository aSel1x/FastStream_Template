meta {
  name: Rotate Secret
  type: http
  seq: 4
}

post {
  url: {{base_url}}/admin/clients/paste-client-id-here/rotate
  body: none
  auth: inherit
}

docs {
  Only meaningful for confidential clients (`is_confidential: true`) — a public client
  has no secret to rotate.
}
